{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Cozmo window.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}